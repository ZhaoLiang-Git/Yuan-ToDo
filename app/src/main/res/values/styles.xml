<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="MyDialog" parent="android:style/Theme.Dialog">
        <item name="android:windowNoTitle">true</item>  <!-- 没有标题-->
        <item name="android:windowBackground">@android:color/transparent</item>   <!-- 背景颜色 -->
        <item name="android:windowFrame">@null</item>   <!-- 没有边框 -->
        <item name="android:windowIsFloating">true</item>   <!-- 窗口浮动 -->
        <item name="android:windowContentOverlay">@null</item>  <!-- 没有阴影 -->
    </style>

    <style name="Title_bar_below_ViewSeparator">
        <item name="android:layout_height">@dimen/DIMEN_1PX</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:background">@color/divider_color</item>
    </style>

    <style name="margin_ViewSeparator">
        <item name="android:layout_height">@dimen/DIMEN_1PX</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:background">#DEDEDE</item>
        <item name="android:layout_marginRight">@dimen/DIMEN_54PX</item>
        <item name="android:layout_marginLeft">@dimen/DIMEN_53PX</item>
    </style>
</resources>
